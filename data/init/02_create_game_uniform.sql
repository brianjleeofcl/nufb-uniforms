CREATE TABLE game_uniform (
    season INTEGER NOT NULL,
    week INTEGER NOT NULL,
    helmet_color VARCHAR,
    helmet_detail VARCHAR,
    jersey_color VARCHAR,
    jersey_letter_color VARCHAR,
    jersey_stripe_detail VARCHAR,
    pants_color VARCHAR,
    pants_detail VARCHAR,
    special VARCHAR,
    tweet_url VARCHAR
);
